rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * DEVELOPMENT MODE RULES
     * Warning: These rules allow anyone with your database reference to read or write data.
     * Use this for starting from scratch and testing connectivity.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}